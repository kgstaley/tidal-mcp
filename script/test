#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if ! command -v uv &>/dev/null; then
  echo "Error: uv is not installed. Run script/bootstrap first." >&2
  exit 1
fi

exec uv run python3 -m pytest "$@"
